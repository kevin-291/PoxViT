data_dir: sorted_dataset
epochs: 30
batch_size: 32
lr: 0.0003
dropout: 0.2
seed: 42
output: artifacts/best_model.pt
milestones: [100, 150]
gamma: 0.1
log_dir: artifacts/logs
log_tag: poxvit
log_freq: 10
checkpoint_freq: 1
save_every_freq: -1
resume: null